/*
    1. Array is linear data structure, it sometime called list, organize items sequentially, 
    that means one after another in memory.
    2. If you want to reduce operation you need use space
    3. Array biggest problems is a it's fixed capacity.
    4. Pointer(memory address)is a location of the data. Through this pointer, you can make custom data structure. Through this first data structure is linked list. JavaScript pointer another name is reference but c, c++ is the pointer.

     
*/